{
  "name": "@agentport/extension",
  "private": true,
  "type": "module",
  "description": "AgentPort wallet as an MV3 Chrome extension: key custody out of the page.",
  "scripts": {
    "build": "tsx build.ts",
    "typecheck": "tsc -p tsconfig.json",
    "check": "tsx build.ts && tsx check.ts"
  },
  "dependencies": {
    "@agentport/client": "0.0.1",
    "@agentport/protocol": "0.0.1",
    "@nisli/core": "^0.54.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287"
  }
}
